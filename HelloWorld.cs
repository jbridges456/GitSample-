using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       Debug.Log("Dance Water Dance");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
